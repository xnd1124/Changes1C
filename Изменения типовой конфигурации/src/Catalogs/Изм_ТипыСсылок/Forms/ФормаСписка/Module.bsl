
&НаСервереБезКонтекста
Процедура СписокПриПолученииДанныхНаСервере(ИмяЭлемента, Настройки, Строки)
	ТипыСсылок = Строки.ПолучитьКлючи();  
	Для каждого  ТипССылки Из ТипыСсылок Цикл
	     СтрокаСписка = Строки[ТипССылки];
		 СтрокаСписка.Данные.АдресПиктограммы =  ПолучитьНавигационнуюСсылку(ТипССылки, "Пиктограмма");
	КонецЦикла;
КонецПроцедуры
